# srt-router — crosspoint-based SRT/NDI router (Rust).
# https://github.com/stoatworks-labs/srt-router
#
#   AV_LAUNCHER_CONFIG=launchers/srt-router.toml npm run tauri dev
#
# srt-router takes `--config <path>` and binds from a NESTED `web.bind` key.

[app]
name = "SRT Router"
command = "/Users/allansargeant/Projects/srt-router/target/debug/srtrouter"
args = ["--config", "{config}"]
url = "http://{host}:{port}/"
default_port = 8080
cwd = "/Users/allansargeant/Projects/srt-router"

[inject]
mode = "configfile"

[inject.configfile]
template = "/Users/allansargeant/Projects/srt-router/config/example.toml"
set_key = "web.bind"
value = "{host}:{port}"

# Palette mirrored from srt-router's web UI (dark slate + periwinkle accent).
[app.theme]
bg = "#14161a"
panel = "#1a1d24"
"panel-2" = "#22262e"
border = "#2a2d33"
text = "#e6e6e6"
muted = "#b7bfca"
dim = "#6b7280"
accent = "#9fb4ff"
"accent-soft" = "#1c2333"
good = "#37835c"
